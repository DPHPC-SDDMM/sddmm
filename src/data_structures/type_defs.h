#pragma once

#include <vector>

namespace SDDMM {
    // can be changed here depending on the future needs (sizes of vectors, 4B vs 8B ints, etc.)
    typedef std::vector<double>::size_type vec_size_t;
}
